version: "3.8"

services:
  n8n:
    image: docker.n8n.io/n8nio/n8n:latest
    container_name: n8n
    restart: unless-stopped
    ports:
      - "127.0.0.1:5678:5678"  # Bind to localhost only; Nginx proxies externally
    environment:
      - N8N_HOST=${N8N_HOST}
      - N8N_PORT=5678
      - N8N_PROTOCOL=${N8N_PROTOCOL:-https}
      - WEBHOOK_URL=${WEBHOOK_URL}
      - GENERIC_TIMEZONE=${TZ:-Asia/Kolkata}
      - TZ=${TZ:-Asia/Kolkata}
      - N8N_BASIC_AUTH_ACTIVE=${N8N_BASIC_AUTH_ACTIVE:-true}
      - N8N_BASIC_AUTH_USER=${N8N_BASIC_AUTH_USER}
      - N8N_BASIC_AUTH_PASSWORD=${N8N_BASIC_AUTH_PASSWORD}
      - N8N_DIAGNOSTICS_ENABLED=false
      - N8N_VERSION_NOTIFICATIONS_ENABLED=false
      - EXECUTIONS_DATA_PRUNE=true
      - EXECUTIONS_DATA_MAX_AGE=168
      - N8N_LOG_LEVEL=info
    volumes:
      - n8n_data:/home/node/.n8n
    user: "node"
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:5678/healthz"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

volumes:
  n8n_data:
    driver: local

# ─────────────────────────────────────────────────────────────────
# OPTIONAL: Postgres backend (uncomment to replace SQLite)
# ─────────────────────────────────────────────────────────────────
# To use Postgres, uncomment the db service below and add these
# environment variables to the n8n service:
#
#   - DB_TYPE=postgresdb
#   - DB_POSTGRESDB_HOST=db
#   - DB_POSTGRESDB_PORT=5432
#   - DB_POSTGRESDB_DATABASE=${POSTGRES_DB:-n8n}
#   - DB_POSTGRESDB_USER=${POSTGRES_USER:-n8n}
#   - DB_POSTGRESDB_PASSWORD=${POSTGRES_PASSWORD}
#
# db:
#   image: postgres:15-alpine
#   container_name: n8n_postgres
#   restart: unless-stopped
#   environment:
#     - POSTGRES_DB=${POSTGRES_DB:-n8n}
#     - POSTGRES_USER=${POSTGRES_USER:-n8n}
#     - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
#   volumes:
#     - postgres_data:/var/lib/postgresql/data
#   healthcheck:
#     test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-n8n}"]
#     interval: 10s
#     timeout: 5s
#     retries: 5
#
# volumes:
#   postgres_data:
